# Production Environment Variables Template
# Copy this file to .env and fill in your actual values

# Database Configuration (Required)
# For Railway/Render, use the DATABASE_URL they provide
# For local Docker: postgres://postgres:yourpassword@db:5432/adaptive_learning
# For Supabase: get from your project settings
DATABASE_URL=postgresql://user:password@host:5432/dbname

# Alternative: Individual database connection parameters
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_NAME=adaptive_learning
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your_password_here

# JWT Configuration (Required)
# Generate a secure random key: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=your-secret-key-at-least-32-characters-long-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# AI Configuration (Optional but recommended for chatbot features)
# Get your API key from https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash

# OpenAI Configuration (Alternative to Gemini, optional)
# OPENAI_API_KEY=your_openai_api_key_here

# Application Configuration
# PORT will be set by the platform (Railway, Render, etc.)
# PORT=8000

# CORS Origins (comma-separated, optional)
# Add your frontend domain(s) here
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
